[package]
name = "photonx-native"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "_photonx_native"
crate-type = ["cdylib", "rlib"]

[features]
default = []
python = ["dep:pyo3"]

[dependencies]
pyo3 = { version = "0.29.2", optional = true, features = ["extension-module"] }
