# Connector-to-IC Paths
Traces shortest connectivity paths from connector components to target ICs through the component/net graph. Paths describe electrical connectivity, not necessarily intended signal flow.
