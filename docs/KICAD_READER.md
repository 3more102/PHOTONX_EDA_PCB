# KiCad reader

The KiCad reader implements a deterministic S-expression subset for nets, segments, vias, footprints, pads, layers and Edge.Cuts. It is intended for round-trip verification, not full KiCad compatibility.
