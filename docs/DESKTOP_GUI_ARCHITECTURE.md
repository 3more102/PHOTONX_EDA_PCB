# Desktop GUI Architecture
Defines framework-independent desktop state for dock panels, document tabs, actions and status. It can back the current Tkinter interface or a future richer desktop frontend.
