# Deterministic Graph Layout
Component placement uses deterministic connectivity layering followed by grid normalization. It favors reproducibility over aesthetic optimality and can be refined without changing editor models.
