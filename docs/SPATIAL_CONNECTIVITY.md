# Spatial Connectivity

A deterministic spatial hash narrows geometry candidate pairs before expensive exact intersection checks. It is a candidate-generation layer and does not replace exact geometry tests.
