# Deterministic Replay and Fault Injection
Event replay is hashed deterministically. Controlled structural faults provide detector-coverage evidence. These tests complement, rather than replace, parser fuzzing and ground-truth regression.
