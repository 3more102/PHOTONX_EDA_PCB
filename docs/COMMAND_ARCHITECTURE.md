# Command Architecture
UI actions can be represented as commands dispatched through a registry and command bus. This keeps user interaction separate from reconstruction algorithms and allows logging/testing.
