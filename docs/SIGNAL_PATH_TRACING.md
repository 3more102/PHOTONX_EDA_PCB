# Signal Path Tracing
Signal paths are computed from the reconstructed bipartite component/net graph. A graph path describes connectivity evidence, not signal direction or functional intent unless separate evidence supports it.
