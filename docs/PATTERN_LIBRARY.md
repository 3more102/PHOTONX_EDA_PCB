# Pattern Library
The pattern library defines small reusable structural expectations such as LED+resistor channels, sensor interfaces, and power stages. Pattern matching is evidence-based and deliberately conservative; it does not execute arbitrary code or infer unsupported intent.
