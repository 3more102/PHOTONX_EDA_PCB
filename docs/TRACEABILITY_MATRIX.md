# Traceability Matrix
Each reconstructed claim can reference source evidence, produced artifacts, review decisions and board/schematic objects. Coverage metrics expose claims that cannot be traced back to evidence.
