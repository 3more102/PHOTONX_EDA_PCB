# Via-span Spatial Search

Via-span resolution builds one pad candidate index per board and reuses it for all drill hits.

Expanded pad bounds are candidate filters. Final center-distance/radius checks remain authoritative. Via span is only marked proven when the drill's plating evidence is explicitly plated.
