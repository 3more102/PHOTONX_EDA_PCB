# Input Protection Paths
Connector-to-target graph paths are annotated with protection components encountered along the path. The path reflects reconstructed connectivity, not guaranteed surge-current flow.
