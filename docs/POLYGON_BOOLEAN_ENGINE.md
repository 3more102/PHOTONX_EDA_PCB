# Polygon Boolean Engine
Uses Shapely-backed union/intersection/difference with deterministic coordinate normalization. It supports reconstruction operations but does not make electrical-semantic claims.
