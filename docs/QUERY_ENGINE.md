# Query Engine
A small deterministic query language supports equality, comparison and substring predicates over dictionaries and model objects. It is intended for inspection and GUI filtering, not arbitrary code execution.
