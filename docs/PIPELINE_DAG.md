# Pipeline DAG
Stages declare required and produced artifacts. The DAG computes dependency order, detects cycles and multiple producers, supports impact analysis, and can reuse cached node outputs when fingerprints match.
