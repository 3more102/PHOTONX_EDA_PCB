# Design Rule Profiles
Profiles define global fabrication constraints with optional per-net overrides. Inferred net classes may suggest a profile, but recovered rules must not be claimed as original rules without evidence.
