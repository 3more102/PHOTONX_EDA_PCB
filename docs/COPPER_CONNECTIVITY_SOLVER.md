# Copper connectivity solver

The solver separates candidate generation, contact evidence, vertical-layer rules and connected-component extraction. Edges retain confidence and reason. A geometric overlap across different copper layers is never sufficient by itself to assert electrical connectivity.
