# Testpoint Inference

Testpoints are candidates derived from exposed copper size, net membership and available metadata. They are not guaranteed manufacturing test locations without external evidence.
