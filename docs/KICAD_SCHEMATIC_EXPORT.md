# KiCad Schematic Export
The current exporter writes a deterministic KiCad schematic subset for reconstructed symbol/label hypotheses. It is intentionally conservative and does not claim complete round-trip parity with every KiCad schematic feature.
