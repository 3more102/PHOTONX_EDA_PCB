# Reverse engineering pipeline

Input discovery → strict parsing → normalized geometry → drill association → physical connectivity → hypotheses → validation → exports. Each stage preserves diagnostics and provenance.
