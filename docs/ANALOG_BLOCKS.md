# Analog Block Candidates
AnalogBlockCandidate stores members, nets, confidence, evidence, optional estimated parameters and explicit assumptions. Candidate types include bias networks, filters, feedback networks, active stages, current-sense structures and oscillator support.
