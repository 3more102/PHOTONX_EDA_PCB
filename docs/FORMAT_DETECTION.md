# Format Detection
Format detection combines file-extension evidence with conservative content signatures. Detection confidence is not parser validation; the selected parser must still validate syntax.
