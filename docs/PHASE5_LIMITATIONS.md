# Phase 5 Limitations
Power-distribution calculations are first-order estimates; graph paths are not functional simulations; BOM and placement reconciliation require reliable reference identifiers. Plugin isolation is logical and does not provide a security sandbox.
