# Event Log
The event log records ordered structured actions and diagnostics without embedding wall-clock time in the deterministic core. Events can be filtered, serialized and replayed into derived state.
