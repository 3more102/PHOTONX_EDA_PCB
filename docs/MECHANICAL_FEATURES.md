# Mechanical Features

Standalone slot and mechanical-hole models provide exact capsule/circle geometry and spatial clearance checks against copper.

They are intentionally separate from BoardModel until high-level Excellon routed-slot parsing can supply complete provenance safely.
