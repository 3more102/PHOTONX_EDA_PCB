# Plugin API
Plugins declare metadata, API version, capabilities and permissions. Parsers, exporters, validators, inference modules, GUI panels and CLI commands can be extended without changing core packages.
