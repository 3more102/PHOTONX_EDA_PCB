# BOM Ingestion

BOM data is optional semantic evidence. Reference designators, values, footprints, manufacturer part numbers and quantities are normalized but never allowed to override conflicting physical evidence silently.
