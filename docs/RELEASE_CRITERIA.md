# Release criteria

A release candidate should pass the supported Python test matrix, strict parser tests, deterministic regression fixtures, configured DRC/ERC and quality gates, export round-trip checks where available, and documented known limitations. Real-board accuracy requires independent ground truth.
