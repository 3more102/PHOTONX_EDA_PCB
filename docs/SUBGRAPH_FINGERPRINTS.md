# Subgraph Fingerprints
Fingerprints use a bipartite component/net topology hash plus a semantic hash derived from component kinds and known net roles. Node IDs are intentionally excluded from canonical hashes so repeated structures with different references can match. Optional relative geometry is compared independently.
