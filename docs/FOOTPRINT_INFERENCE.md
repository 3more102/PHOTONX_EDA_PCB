# Footprint Inference

Footprint inference uses pad clustering, count, drilling fraction, bounding-box aspect and orientation. Results remain candidates and must not be promoted to exact part numbers without external evidence.
