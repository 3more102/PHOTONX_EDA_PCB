# KiCad Schematic Round-trip
The reader extracts header, root UUID, symbols, wires and labels from the generated subset. Canonical comparison ignores UUID differences and rounds schematic coordinates to KiCad's documented 0.0001 mm resolution.
