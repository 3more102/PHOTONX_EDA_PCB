# Aperture Macro AST

PHOTONX keeps aperture-macro syntax, expression evaluation, primitive recognition, and validation as separate layers. This package complements the lower-level Gerber macro helpers without replacing them.
