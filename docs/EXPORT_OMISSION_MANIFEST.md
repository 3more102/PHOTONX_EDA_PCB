# Export omission manifest

An export can be syntactically valid while intentionally omitting unresolved evidence.

The omission manifest lists exported slot IDs, skipped slot IDs and a reason for every skipped slot. This prevents unresolved or unsupported mechanical evidence from disappearing silently.
