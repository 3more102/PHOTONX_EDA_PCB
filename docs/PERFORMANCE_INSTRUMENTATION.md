# Performance Instrumentation

Parser and reconstruction stages expose deterministic counters, timers, resource budgets, and sampling helpers so performance work can be measured without changing reconstruction semantics.
