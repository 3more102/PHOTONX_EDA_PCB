# Repeated Circuits
Repeated-circuit groups require at least two topology-compatible instances and a configurable fingerprint similarity threshold. Each instance records members, similarity, and explicit differences. Groups produce evidence records and ambiguous groups can enter human review.
