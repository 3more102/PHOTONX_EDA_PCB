# Schematic Hypotheses

PHOTONX may derive a physical connectivity netlist and schematic hypotheses. It cannot recreate the original schematic layout, symbols, values or semantic net labels when those facts are absent from manufacturing data.
